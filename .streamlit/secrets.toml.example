# Copy into Streamlit Community Cloud → App settings → Secrets
# or to .streamlit/secrets.toml locally (never commit secrets.toml).

OPENAI_API_KEY = "sk-..."
LLM_MODEL = "gpt-4.1-mini"
DEMO_QUERY_LIMIT = 3
